use async_trait::async_trait;

use crate::domain::{{TEMPLATE_NAME}}::Repository;

/// Impl is an implementation of the `Repository` trait that
/// connects to a data source.
pub struct Impl {
    // In a real app, you might want to inject a database connection here.
    // db: Option<T>,
}

impl Impl {
    pub fn new() -> Self {
        Self { }
    }
}

#[async_trait]
impl Repository for Impl {}
